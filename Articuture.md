Now let me create a comprehensive PRD + Prompt document for this project.Now I have everything I need. Let me build the PRD as a beautiful HTML artifact and also create the docx.Now I have all the context I need. Let me create the comprehensive PRD + Prompt document.Now let me create the interactive visual version of the PRD as an HTML artifact for easy reading:Here's your full PRD + Builder Prompt. Here's what's packed into the document:

**Sections covered:**
1. **Executive Summary** — what it is, why it exists, the core pitch
2. **Problem Statement** — the gap it fills, who it's for
3. **Product Vision & Goals** — vision statement + 6-month success metrics table
4. **UX Design** — full 9-step user flow, panel animation logic, thinking state options, theme system
5. **Feature Specification** — priority matrix (P0→P3) covering all 17 features across 3 phases
6. **Technical Architecture** — full tech stack table, AI model recommendation, DB schema, all 9 API routes, AI prompt engineering schema
7. **Project File Structure** — complete folder tree with every file explained
8. **Environment Variables** — ready-to-paste `.env.local`
9. **The Builder Prompt** — copy-paste ready, ~120 lines, covers everything from layout transitions to React Flow custom nodes to Ollama system prompt to keyboard shortcuts
10. **Phased Delivery Plan** — 4 phases, weeks 1–12
11. **Risks & Mitigations** — 5 key risks with mitigations
12. **Appendix** — exact install commands

**On the AI model:** I'd recommend swapping from minimax-m2.5 to **`llama3.1:70b`** — it's significantly stronger at structured JSON output (which is critical for generating valid React Flow graphs). If your machine can't handle 70B, `mistral-nemo` or `deepseek-r1:14b` are the next best free options on Ollama. The builder prompt already references this.